# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.omc/
.vercel/
next.user-config.*

# Environment variables — never commit real secrets
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development
# Keep only *.example files (safe to commit)
!.env*.example

# Common ignores
node_modules
.next/
.DS_Store
components/forms/event-registration.types.ts
tsconfig.tsbuildinfo

# SQLite database (contains user data — never commit)
data/

# Cloudflare / OpenNext build artifacts
.open-next/
.wrangler/
